mode: tryout    # train, test, tryout

train:
  batch_size: 32
  epochs: 10

test:
  metrices: []

model:
  name: resnet

data:
  name: "paris"  # paris, fipm
  path: "C:/Users/tippolito/Data/Benchmark"






